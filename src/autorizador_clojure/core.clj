(ns autorizador-clojure.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))

;; nessa camada pretendo importar o diplomat e fazer algumas chamadas de exemplo de uso
