(ns autorizador-clojure.models.authorization-result)

;; essa camada deve APENAS definir o schema do retorno da authorization pelo domínio usando a biblioteca plumatic
;; nao realizar nenhuma outra operaçao sobre a entidade nessa camada, apenas definir a estrutura dos dados/schema