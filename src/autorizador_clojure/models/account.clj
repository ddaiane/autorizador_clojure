(ns autorizador-clojure.models.account)


;; essa camada deve APENAS definir o schema da entidade account dentro do domínio usando a biblioteca plumatic
;; nao realizar nenhuma outra operaçao sobre a entidade nessa camada, apenas definir a estrutura dos dados/schema