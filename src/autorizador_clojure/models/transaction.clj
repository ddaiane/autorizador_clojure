(ns autorizador-clojure.models.transaction)


;; essa camada deve APENAS definir o schema da entidade transaction dentro do domínio usando a biblioteca plumatic
;; nao realizar nenhuma outra operaçao sobre a entidade nessa camada, apenas definir a estrutura dos dados/schema